name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
        
      - uses: actions/upload-artifact@master
        with:
          name: blank_file
          path: blank_file
